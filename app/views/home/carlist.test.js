'use strict';

describe('ct.views module', function() {

  beforeEach(module('ct.views'));

  describe('carList controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var clctrl = $controller('CarListController');
      expect(clctrl).toBeDefined();
    }));

  });
});